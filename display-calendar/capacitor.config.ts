import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.displaycalendar',
  appName: 'display-calendar',
  webDir: 'dist'
};

export default config;
