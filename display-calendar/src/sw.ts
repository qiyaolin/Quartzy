// Registerable service worker placeholder for Vite PWA
// The plugin will generate the actual service worker at build time.

self.addEventListener('install', () => {
  // no-op
});

self.addEventListener('activate', () => {
  // no-op
});



